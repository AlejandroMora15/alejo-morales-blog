---
interface Props {
  code: string;
}

const { code } = Astro.props;
---

<pre
  class='language-js'>
<code class='language-js shadow-md text-xs sm:text-sm inline-flex text-left items-center bg-zinc-800 text-zinc-50 rounded-lg p-4 pl-6 w-full'>{code}</code>
</pre>
